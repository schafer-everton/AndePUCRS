<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MapActivity">

    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.pucrs.andepucrs.controller.MapsActivity" />
    <ImageView
        android:id="@+id/pointer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|left"
        android:layout_centerInParent="true"
        android:src="@drawable/android_pointer" />
    <Button
        android:id="@+id/favoriteButton"
        android:layout_width="30dp"
        android:layout_height="wrap_content"
        android:layout_gravity="left|bottom"
        android:background="@null"
        android:drawableLeft="@drawable/star_icon" />

    <Button
        android:id="@+id/commentButton"
        android:layout_width="30dp"
        android:layout_height="wrap_content"
        android:layout_gravity="right|bottom"
        android:background="@null"
        android:drawableLeft="@drawable/cloud_icon" />


    <Button
        android:id="@+id/markerButton"
        android:layout_width="20dp"
        android:layout_height="wrap_content"
        android:background="@null"
        android:drawableLeft="@drawable/google_marker_small"
        android:layout_gravity="right|center_vertical" />

    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/mapProgressBar"
        android:layout_gravity="center"
        android:indeterminate="false" />

    <Button
        android:layout_width="30dp"
        android:layout_height="wrap_content"
        android:background="@null"
        android:drawableLeft="@drawable/recalculate_icon"
        android:id="@+id/recalculateButton"
        android:layout_gravity="left|center_vertical" />

</FrameLayout>